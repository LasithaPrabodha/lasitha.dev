<span transition:name="me" class="my-name font-bold max-sm:block">Lasitha Prabodha</span>

<style>
  .my-name {
    font-family: "Poppins", sans-serif;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    -webkit-background-clip: text;
    background-image: linear-gradient(45deg, var(--clr-grd-one), var(--clr-grd-two), var(--clr-grd-one));

    background-size: 300%;
    background-position: -100%;
    animation: animatedText 10s infinite alternate-reverse;
  }
  @keyframes animatedText {
    to {
      background-position: 100%;
    }
  }
</style>
