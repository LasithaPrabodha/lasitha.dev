---
import Page from "../layouts/Page.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import { Image } from "astro:assets";

import myPhone from "../assets/images/casual-photo.jpg";
import RectPattern from "../components/RectPattern.astro";
---

<Page title={SITE_TITLE} description={SITE_DESCRIPTION} addNight={true} class="about center">
  <div class="about__container">
    <div class="name">
      <small>About</small>
      <h2>Lasitha Prabodha Weligampola</h2>
    </div>
    <div class="details">
      <div class="details__summary">
        <p>Hi there!</p>
        <p>
          I'm excited to introduce myself! I've been writing software for over 7 years, and I love taking my passion and
          turning it into real results. I build strong web and mobile apps, especially for travel, hospitality, and
          customer management.
        </p>
        <p>
          Some of my go-to languages and tools for web & mobile development: Typescript, Angular, React, Vue, React
          Native, Kotlin, Swift, ASP.NET Core, Azure, SQL, Firebase, Docker, SASS, Bootstrap, TailwindCSS, Jest.
        </p>
        <p>I'm always learning new things as the tech world keeps changing, which keeps me excited!</p>
      </div>
      <div class="details__photo">
        <Image src={myPhone} alt="A portrai photo" width={300} height={300} />
        <RectPattern />
      </div>
    </div>
  </div>
</Page>

<style>
  small {
    text-transform: uppercase;
  }
  .about__container {
    text-align: start;
    margin-bottom: 10em;
  }

  .name h2 {
    -webkit-text-fill-color: transparent;
    background-clip: text;
    -webkit-background-clip: text;
    background-image: linear-gradient(45deg, var(--clr-grd-one), var(--clr-grd-two));
  }

  .details {
    display: flex;
    max-width: 800px;
    margin-top: 2em;
    gap: 1em;
  }

  .details__summary {
    flex: 1;
  }

  .details__photo {
    position: relative;
  }

  :global(.details__photo > svg) {
    position: absolute;
    right: 0;
    top: 0;
    margin-right: -3rem;
    margin-top: 1rem;
  }

  img {
    border-radius: 8px;
    box-shadow: var(--shadow);
    z-index: 1;
    position: relative;
  }

  @media screen and (min-width: 599px) {
    img {
      margin-top: 3em;
    }
  }

  @media (max-width: 600px) {
    .details {
      display: block;
    }

    img {
      width: 100%;
      height: 100%;
    }
  }
</style>
