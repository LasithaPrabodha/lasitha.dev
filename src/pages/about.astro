---
import Page from "../layouts/Page.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import { Image } from "astro:assets";

import myPhoto from "../assets/images/casual-photo.jpg";
import RectPattern from "../components/RectPattern.astro";
---

<Page
  title={"About - " + SITE_TITLE}
  description={SITE_DESCRIPTION}
  addNight={true}
>
  <div
    class="flex flex-1 flex-col p-6 w-full m-auto box-border mt-6 max-w-[900px] text-start mb-40 max-sm:mb-8"
  >
    <div class="name">
      <small class="uppercase">About</small>
      <h2 class="font-semibold">Lasitha Prabodha Weligampola</h2>
    </div>
    <div class="flex max-sm:block mt-8 gap-4">
      <div class="flex-1">
        <p>Hi there!</p>
        <p>
          I'm excited to introduce myself! I've been writing software for over 7
          years, and I love taking my passion and turning it into real results.
          I build strong web and mobile apps, especially for travel,
          hospitality, and customer management.
        </p>
        <p>
          Some of my go-to languages and tools for web & mobile development:
          Typescript, Angular, React, Vue.js, React Native, Redux, Flutter,
          Kotlin, Swift, ASP.NET Core, Node.js, Azure, SQL, Firebase, Docker,
          SASS, Bootstrap, TailwindCSS, Jest.
        </p>
        <p>
          I'm always learning new things as the tech world keeps changing, which
          keeps me excited!
        </p>
      </div>
      <div class="details__photo relative">
        <Image
          src={myPhoto}
          loading="eager"
          alt="My portrait photo"
          width={300}
          height={300}
        />
        <RectPattern />
      </div>
    </div>
  </div>
</Page>

<style>
  .name h2 {
    -webkit-text-fill-color: transparent;
    background-clip: text;
    -webkit-background-clip: text;
    background-image: linear-gradient(
      45deg,
      var(--clr-grd-one),
      var(--clr-grd-two)
    );
  }

  :global(.details__photo > svg) {
    position: absolute;
    right: 0;
    top: 0;
    margin-right: -3rem;
    margin-top: 1rem;
    z-index: -1;
  }

  img {
    border-radius: 8px;
    box-shadow: var(--shadow);
    position: relative;
  }

  @media screen and (min-width: 639px) {
    img {
      margin-top: 3em;
    }
  }

  @media (max-width: 640px) {
    .details {
      display: block;
    }

    img {
      width: 100%;
      height: 100%;
    }
  }
</style>
